# Copiez ce fichier vers .env.local pour pointer l’app vers le service de sauvegarde.
# Le token Turso reste côté Worker (jamais embarqué dans l’app publique).
EXPO_PUBLIC_CLOUDFLARE_WORKER_URL=https://merchanthq-backup.lirostudio.workers.dev